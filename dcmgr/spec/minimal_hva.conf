# This is a temporary workaround.
# hva.conf expects the vm_data_dir to exist and currently we are not able to
# skip this validation check in the unit tests. We need both an existing
# directory and another directory named 'tmp' inside it so we set it to root

vm_data_dir '/'
