#
# Distribution dependent scirpt template for wakame-vdc.
#

#
# In this function, install packages which requires for wakame-vdc,
# such as MySQL, ruby, rubygems and bundler
#

# Write script to install packages that wakame-vdc required.
# wakame.sh (actually wakame_lib.sh) calls it in './wakame.sh install'
# Note:
#   - bundle is not required because bundle will be installed later
function do_install{
}
