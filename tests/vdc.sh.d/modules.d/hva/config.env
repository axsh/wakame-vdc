#!/bin/bash

# IP address or host name where hva runs.
host=${host:?"required variable: host"}
# Unique ID for hva.
hva_id=${hva_id:?"required variable: hva_id"}
hypervisor=${hypervisor:?"required variable: hypervisor"}
amqp_server_uri=${amqp_server_uri:?"required variable: amqp_server_uri"}
