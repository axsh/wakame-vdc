<div id="notification">
  <div class="title clearfix">
    <h3><%= t("notification.header") %></h3>
    <div class="maincontent">
      <ul id="notifications">
      <% @notifications.each do |notification| %>
      <li><b><%= notification.title %></b> <div class="notification_date"><%= notification.publish_date_from %> ~ <%= notification.publish_date_to %></div><hr /><%= notification.article %></li>
      <% end %>
      </ul>
    </div>
  </div>
</div>
