# -*-Shell-script-*-
#
# 11.12
#

function run_cmd() {
  local namespace=$1 cmd=$2

  [[ -f ${BASH_SOURCE[0]%/*}/v${api_version}.d/${namespace}.sh ]] && {
    .   ${BASH_SOURCE[0]%/*}/v${api_version}.d/${namespace}.sh
  } || {
    echo "$0 [namespace] [cmd]" >&2
    return 1
  }
}
